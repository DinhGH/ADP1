import React, { useState } from 'react';  function TotalCostModal() {   const [price, setPrice] = useState('');   const [quantity, setQuantity] = useState('');   const [total, setTotal] = useState(null);    const handleCalculate = () => {     const p = Number(price);     const q = Number(quantity);     if (!isNaN(p) && !isNaN(q)) {       setTotal(p * q);     } else {       setTotal('Invalid input');     }   };    const styles = {     container: { textAlign: 'center' },     input: { padding: '10px', width: '150px', margin: '5px', fontSize: '16px' },     button: {       padding: '10px 20px', fontSize: '16px',       backgroundColor: '#ffc107', color: 'black',       border: 'none', borderRadius: '5px', cursor: 'pointer'     },     result: { marginTop: '15px', fontSize: '18px' }   };    return (     <div style={styles.container}>       <h2>Tính thành tiền</h2>       <input type="number" value={price} onChange={e => setPrice(e.target.value)} placeholder="Đơn giá" style={styles.input} />       <input type="number" value={quantity} onChange={e => setQuantity(e.target.value)} placeholder="Số lượng" style={styles.input} />       <br />       <button onClick={handleCalculate} style={styles.button}>Tính</button>       <div style={styles.result}>Thành tiền: {total}</div>     </div>   ); }  export default TotalCostModal; 