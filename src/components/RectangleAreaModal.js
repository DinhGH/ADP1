import React, { useState } from 'react';  function RectangleAreaModal() {   const [width, setWidth] = useState('');   const [height, setHeight] = useState('');   const [area, setArea] = useState(null);    const handleSubmit = () => {     const w = Number(width);     const h = Number(height);     if (!isNaN(w) && !isNaN(h)) {       setArea(w * h);     }   };    const styles = {     container: { textAlign: 'center' },     input: { padding: '10px', width: '150px', margin: '5px', fontSize: '16px' },     button: { ...buttonStyle },     result: { marginTop: '15px', fontSize: '18px', color: '#333' }   };    const buttonStyle = {     padding: '10px 20px',     fontSize: '16px',     backgroundColor: '#007bff',     color: '#fff',     border: 'none',     borderRadius: '5px',     cursor: 'pointer',   };    return (     <div style={styles.container}>       <h2>Rectangle Area</h2>       <input type="text" placeholder="Width" value={width} onChange={(e) => setWidth(e.target.value)} style={styles.input} />       <input type="text" placeholder="Height" value={height} onChange={(e) => setHeight(e.target.value)} style={styles.input} />       <br />       <button onClick={handleSubmit} style={buttonStyle}>Calculate</button>       <div style={styles.result}>Area: {area}</div>     </div>   ); }  export default RectangleAreaModal;