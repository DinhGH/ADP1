import React, { useState } from 'react';  function SubtractModal() {   const [a, setA] = useState('');   const [b, setB] = useState('');   const [result, setResult] = useState(null);    const handleSubmit = () => {     const numA = Number(a);     const numB = Number(b);     if (!isNaN(numA) && !isNaN(numB)) {       setResult(numA - numB);     }   };    const styles = {     container: { textAlign: 'center' },     input: { padding: '10px', width: '150px', margin: '5px', fontSize: '16px' },     button: { ...buttonStyle },     result: { marginTop: '15px', fontSize: '18px', color: '#333' }   };    const buttonStyle = {     padding: '10px 20px',     fontSize: '16px',     backgroundColor: '#007bff',     color: '#fff',     border: 'none',     borderRadius: '5px',     cursor: 'pointer',   };    return (     <div style={styles.container}>       <h2>Subtraction</h2>       <input type="text" placeholder="Enter number A" value={a} onChange={(e) => setA(e.target.value)} style={styles.input} />       <input type="text" placeholder="Enter number B" value={b} onChange={(e) => setB(e.target.value)} style={styles.input} />       <br />       <button onClick={handleSubmit} style={buttonStyle}>Submit</button>       <div style={styles.result}>Result: {result}</div>     </div>   ); }  export default SubtractModal; 