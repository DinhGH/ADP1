import React, { useState } from 'react'; function WaterBillModal() { const [start, setStart] = useState(''); const [end, setEnd] = useState(''); const [total, setTotal] = useState(null); const handleSubmit = () => { const s = Number(start); const e = Number(end); if (!isNaN(s) && !isNaN(e) && e >= s) { setTotal((e - s) * 10000); } }; const styles = { container: { textAlign: 'center' }, input: { padding: '10px', width: '150px', margin: '5px', fontSize: '16px' }, button: { ...buttonStyle }, result: { marginTop: '15px', fontSize: '18px', color: '#333' } }; const buttonStyle = { padding: '10px 20px', fontSize: '16px', backgroundColor: '#007bff', color: '#fff', border: 'none', borderRadius: '5px', cursor: 'pointer', }; return (<div style={styles.container}>       <h2>Water Bill</h2>       <input type="text" placeholder="Start number" value={start} onChange={(e) => setStart(e.target.value)} style={styles.input} />       <input type="text" placeholder="End number" value={end} onChange={(e) => setEnd(e.target.value)} style={styles.input} />       <br />       <button onClick={handleSubmit} style={buttonStyle}>Calculate</button>       <div style={styles.result}>Total: {total} VND</div>     </div>); } export default WaterBillModal; 