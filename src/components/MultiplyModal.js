import React, { useState } from 'react'; function MultiplyModal() { const [a, setA] = useState(''); const [b, setB] = useState(''); const [result, setResult] = useState(null); const handleMultiply = () => { const numA = Number(a); const numB = Number(b); if (!isNaN(numA) && !isNaN(numB)) { setResult(numA * numB); } else { setResult('Invalid input'); } }; const styles = { container: { textAlign: 'center' }, input: { padding: '10px', width: '150px', margin: '5px', fontSize: '16px' }, button: { padding: '10px 20px', fontSize: '16px', backgroundColor: '#007bff', color: 'white', border: 'none', borderRadius: '5px', cursor: 'pointer' }, result: { marginTop: '15px', fontSize: '18px' } }; return (<div style={styles.container}>       <h2>Phép nhân hai số</h2>       <input type="number" value={a} onChange={e => setA(e.target.value)} placeholder="Số A" style={styles.input} />       <input type="number" value={b} onChange={e => setB(e.target.value)} placeholder="Số B" style={styles.input} />       <br />       <button onClick={handleMultiply} style={styles.button}>Tính</button>       <div style={styles.result}>Kết quả: {result}</div>     </div>); } export default MultiplyModal; 