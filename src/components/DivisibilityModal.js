import React, { useState } from 'react';  function DivisibilityModal() {   const [a, setA] = useState('');   const [b, setB] = useState('');   const [result, setResult] = useState('');    const handleCheck = () => {     const numA = Number(a);     const numB = Number(b);     if (!isNaN(numA) && !isNaN(numB) && numB !== 0) {       setResult(numA % numB === 0 ? 'A chia hết cho B' : 'A không chia hết cho B');     } else {       setResult('Invalid input');     }   };    const styles = {     container: { textAlign: 'center' },     input: { padding: '10px', width: '150px', margin: '5px', fontSize: '16px' },     button: {       padding: '10px 20px', fontSize: '16px',       backgroundColor: '#dc3545', color: 'white',       border: 'none', borderRadius: '5px', cursor: 'pointer'     },     result: { marginTop: '15px', fontSize: '18px' }   };    return (     <div style={styles.container}>       <h2>Kiểm tra chia hết</h2>       <input type="number" value={a} onChange={e => setA(e.target.value)} placeholder="Số A" style={styles.input} />       <input type="number" value={b} onChange={e => setB(e.target.value)} placeholder="Số B" style={styles.input} />       <br />       <button onClick={handleCheck} style={styles.button}>Kiểm tra</button>       <div style={styles.result}>{result}</div>     </div>   ); }  export default DivisibilityModal;